{% extends "::base.html.twig" %}
{% block title %}{{ "Add new category"|trans }}{% endblock %}
{% block body %}

    {{ form_start(add_form) }}
    {{ form_widget(add_form) }}
    <input type="submit" value="{{ "Edit category"|trans }}" class="submit-btn" />
    {{ form_end(add_form) }}
{% endblock %}
